# Legacy Admin

## Purpose
- Older Angular admin application.

## Working Rules
- Prefer `src/admin` for current admin feature work unless the task explicitly targets this app.
- Preserve existing Angular 11-era patterns; avoid partial modern rewrites in isolated files.
- Do not edit generated output or dependency folders.

## Validation
- Use the local Angular scripts in `package.json` when legacy changes are required.
